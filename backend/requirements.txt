Django==5.1.15
djangorestframework==3.17.1
django-cors-headers==4.9.0

# Production serving / infra (engaged via env in settings.py; dev still runs on
# SQLite + locmem with none of these configured).
gunicorn==23.0.0
psycopg[binary]==3.2.3        # PostgreSQL driver (Django 5.1)
dj-database-url==2.3.0        # parse DATABASE_URL -> DATABASES['default']
django-redis==5.4.0           # Redis cache backend
whitenoise==6.8.2             # serve admin/stats static from gunicorn
python-dotenv==1.2.2          # autoload backend/.env into the environment

# Monitoring (production; inert in dev until env is set — see deploy/monitoring/).
django-prometheus==2.3.1      # app metrics + /metrics view + DB/middleware hooks
prometheus-client==0.21.1     # pinned; multiprocess API used by the gunicorn hooks
sentry-sdk==2.20.0            # GlitchTip (Sentry-compatible) error tracking; DjangoIntegration

# Transitive (pinned for reproducibility)
asgiref==3.11.1
sqlparse==0.5.5

# Later milestone (Stripe "remove ads") — uncomment when implementing:
# stripe==10.*
