Django==5.1.15
djangorestframework==3.17.1
django-cors-headers==4.9.0

# Transitive (pinned for reproducibility)
asgiref==3.11.1
sqlparse==0.5.5

# Later milestone (Stripe "remove ads") — uncomment when implementing:
# stripe==10.*
