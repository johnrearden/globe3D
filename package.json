{
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "earcut": "^3.0.2",
    "flag-icons": "^7.5.0",
    "three": "0.128.0",
    "world-geojson": "^3.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "pngjs": "^7.0.0",
    "simplify-js": "^1.2.4",
    "vitest": "^4.1.8",
    "world-countries": "^5.1.0"
  },
  "scripts": {
    "dev": "node dev-server.mjs",
    "dev:web": "npm --workspace @terragotcha/web run dev",
    "build:globe": "node build-textures.js",
    "build:geo-data": "node backend/data_build/build_countries_json.mjs && node backend/data_build/build_mesh_iso.mjs",
    "build:assets": "node compress-assets.mjs",
    "build:tokens": "node packages/design-tokens/bin/build-tokens.mjs",
    "build:sitemap": "node build-sitemap.mjs",
    "build:landing-facts": "node build-landing-facts.mjs",
    "build:pages": "node build-sitemap.mjs && node build-landing-facts.mjs && npm run build:web && node build-pages.mjs",
    "build:pages:local": "node build-sitemap.mjs && node build-landing-facts.mjs && npm --workspace @terragotcha/web run build:local && node build-pages.mjs && ln -sfn ../assets dist/assets",
    "build:web": "npm --workspace @terragotcha/web run build",
    "check:syntax": "node check-syntax.mjs",
    "check:tokens": "node check-tokens.mjs",
    "test": "node check-syntax.mjs && node check-tokens.mjs && node packages/design-tokens/bin/build-tokens.mjs --check && node build-landing-facts.mjs --check && vitest run",
    "test:watch": "vitest"
  }
}
